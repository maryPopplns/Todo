import "../index.css";
import {
  HEADER,
  MENU_BUTTON,
  META_DATA,
  NAV_BAR,
  ADD_GROUP_INPUT,
} from "../dom/dom.js";
import { EVENT_LISTENERS } from "./event_listeners.js";

META_DATA();
HEADER();
NAV_BAR();
ADD_GROUP_INPUT();
MENU_BUTTON();
EVENT_LISTENERS();
